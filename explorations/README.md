# explorations/

Throwaway analysis — notebooks, scratch SQL, one-off charts. Notebooks are
gitignored (`*.ipynb`) unless named `*.example.ipynb`. When something here turns
into a finding, promote it: a `releases/` dataset or an `apps/` visualization,
with the question marked `shipped` in `questions/backlog.csv`.
